# Core dependencies
requests>=2.31.0
feedparser>=6.0.10
beautifulsoup4>=4.12.0
lxml>=5.0.0
PyMuPDF>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0
markdown>=3.5.0
pymysql>=1.1.0

# API server (optional — for web dashboard chat & REST API)
fastapi>=0.110.0
uvicorn>=0.29.0

# Streamlit dashboard (optional — for interactive web dashboard)
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0

# ML / NLP (optional — for risk prediction and sentiment analysis)
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0
vaderSentiment>=3.3.0

# Testing (optional — for running the test suite)
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
pytest-cov>=5.0.0

# Phase 1: Opportunity Intelligence Platform
# Real-time ingestion (optional — Kafka disabled if not installed)
kafka-python-ng>=2.2.0
redis>=5.0.0
# Signal collectors
PyGithub>=2.1.0
# Note: sec-edgar-downloader is optional (SEC collector works with RSS fallback)
